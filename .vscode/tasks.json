{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "detail": "Run and build TicTacToe",
      "command": "cd build && cmake --build . && ./TicTacToe",
      "windows": {
        "command": "cd build && cmake --build . && TicTacToe.exe"
      },
      "group": "build",
      "problemMatcher": "$gcc"
    }
  ]
}
